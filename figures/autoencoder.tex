\documentclass{standalone}
\usepackage{tikz}
\begin{document}
    
\begin{tikzpicture}
    \path (0,0) node[draw,,minimum width=2cm,minimum height=10cm] (encoder){encoder};
    \path (-2,0) node[rotate=90] (input){Input};
    \path (input) edge[->,thick] (encoder);
    \path (4,0) node[draw,minimum width=2cm,minimum height=5cm](code){Code};
    \path (8,0) node[draw,,minimum width=2cm,minimum height=10cm] (decoder){Decoder};
    \path (encoder.north east) edge (code.north west);
    \path (encoder.south east) edge (code.south west);
    \path (code.north east) edge (decoder.north west);
    \path (code.south east) edge (decoder.south west);
    \path (12,0) node(output) {output};
    \path (decoder) edge [->,thick] (output);
    \end{tikzpicture}
    \end{document}

